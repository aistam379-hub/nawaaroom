rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // نموذج أولي: أي مستخدم مسجّل يقرأ/يكتب. شدّدها لاحقًا بحسب الملكية.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
